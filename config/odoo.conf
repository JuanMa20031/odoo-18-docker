[options]
; Puerto en el que se ejecutará Odoo
http_port = 8069

; Configuración de la base de datos
db_name = ${POSTGRES_DB}
db_user = ${POSTGRES_USER}
db_password = ${POSTGRES_PASSWORD}
db_host = ${POSTGRES_HOST}
db_port = ${POSTGRES_PORT}

; Directorio de complementos (mover al volumen /var/lib/odoo)
addons_path = /var/lib/odoo/extra-addons

; Ruta para guardar datos persistentes de Odoo
data_dir = /var/lib/odoo

; Configuración del registro (mover al volumen /var/lib/odoo)
logfile = /var/lib/odoo/logs/odoo.log
log_level = info

; Configuración para desarrolladores (opcional)
developer_mode = True